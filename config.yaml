server:
  port: 8080
  mode: debug  # debug / release

mysql:
  host: 127.0.0.1
  port: 3306
  user: root
  password: "admin123"
  database: recipe_app

jwt:
  secret: "recipe-app-jwt-secret-change-in-production"
  expire_hours: 720  # 30 days

wechat:
  appid: "wx6728bb699338278c"
  secret: ""  # 填入小程序 secret

oss:
  endpoint: "oss-cn-hangzhou.aliyuncs.com"
  access_key_id: ""
  access_key_secret: ""
  bucket: ""
  region: "cn-hangzhou"
  custom_domain: ""  # 如绑定域名，留空则用OSS默认域名

ai:
  api_key: ""  # DeepSeek API key
  base_url: "https://api.deepseek.com"
  model: "deepseek-chat"
